# @package experiment.config.monty_config.learning_modules

# HSV + LTP learning module: identical to evidence_1lm_nn5_dod0025_ltp EXCEPT the
# HSV feature keeps its baseline weight [2, 0.5, 0.5] instead of being zeroed. This
# makes the arm "HSV + LTP" so it can be paired against the HSV-only baseline
# (evidence_1lm_nn5_dod0025) to test whether LTP ADDS signal on top of color.
# LTP weight = 2.0, matching the rest of the study.

defaults:
  - hypotheses_updater/burst_sampling_5nn@learning_module_0

learning_module_0:
  _target_: tbp.monty.frameworks.models.evidence_matching.learning_module.EvidenceGraphLM
  feature_weights:
    patch:
      hsv: ${np.array:[2, 0.5, 0.5]}
      pose_vectors: ${np.ones:3}
      principal_curvatures_log: ${np.ones:2}
      ltp: 2.0
  tolerances:
    patch:
      hsv: ${np.array:[0.1, 0.2, 0.2]}
      principal_curvatures_log: ${np.ones:2}
      ltp: 0.50
  max_match_distance: 0.01
  x_percent_threshold: 20
  evidence_threshold_config: all
  max_graph_size: 0.3
  num_model_voxels_per_dim: 100
  gsg:
    _target_: tbp.monty.frameworks.models.goal_generation.EvidenceGoalGenerator
    goal_tolerances:
      location: 0.015
    elapsed_steps_factor: 10
    min_post_goal_success_steps: 5
    x_percent_scale_factor: 0.75
    desired_object_distance: 0.025
